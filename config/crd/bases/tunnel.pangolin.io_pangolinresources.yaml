---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pangolinresources.tunnel.pangolin.io
spec:
  group: tunnel.pangolin.io
  names:
    kind: PangolinResource
    listKind: PangolinResourceList
    plural: pangolinresources
    shortNames:
    - presource
    singular: pangolinresource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.resourceId
      name: Resource ID
      type: string
    - jsonPath: .spec.protocol
      name: Protocol
      type: string
    - jsonPath: .spec.httpConfig.subdomain
      name: Subdomain
      type: string
    - jsonPath: .status.fullDomain
      name: Full Domain
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.bindingMode
      name: Binding Mode
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PangolinResource is the Schema for the pangolinresources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PangolinResourceSpec defines the desired state of PangolinResource
            properties:
              enabled:
                default: true
                description: Enable/disable this resource
                type: boolean
              httpConfig:
                description: HTTP-specific configuration
                properties:
                  blockAccess:
                    description: |-
                      BlockAccess blocks access until user is authenticated
                      Only effective when SSO is enabled
                    type: boolean
                  domainId:
                    description: 'OPTION 1: Domain ID to use (e.g., "domain1")'
                    type: string
                  domainName:
                    description: |-
                      OPTION 2: Domain name to use (e.g., "yourdomain.com") - NEW
                      If specified, will be resolved to domainId by the operator
                    type: string
                  sso:
                    description: SSO enables SSO authentication for this resource
                    type: boolean
                  subdomain:
                    description: Subdomain for this resource
                    minLength: 1
                    type: string
                required:
                - subdomain
                type: object
              name:
                description: Resource configuration for NEW resources
                type: string
              protocol:
                type: string
              proxyConfig:
                description: TCP/UDP-specific configuration
                properties:
                  enableProxy:
                    default: true
                    description: Enable proxy functionality
                    type: boolean
                  proxyPort:
                    description: Proxy port to expose
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - proxyPort
                type: object
              resourceId:
                description: |-
                  BINDING MODE: Resource ID to bind to existing resource
                  If specified, will bind to existing resource instead of creating new one
                type: string
              siteRef:
                description: |-
                  SiteRef directly references a Pangolin site
                  If specified, takes precedence over TunnelRef for site resolution
                properties:
                  niceId:
                    description: NiceID is the human-readable site identifier
                    type: string
                  siteId:
                    description: SiteID is the numeric site identifier
                    type: integer
                type: object
              targets:
                description: Targets configuration - multiple targets for path-based
                  routing
                items:
                  description: TargetConfig defines the backend target
                  properties:
                    ip:
                      description: Target IP or hostname
                      type: string
                    method:
                      default: http
                      description: Target method/protocol
                      enum:
                      - http
                      - https
                      - tcp
                      - udp
                      type: string
                    path:
                      description: Path to match for routing (e.g., "/api")
                      type: string
                    pathMatchType:
                      description: 'PathMatchType defines how to match the path: exact,
                        prefix, or regex'
                      enum:
                      - exact
                      - prefix
                      - regex
                      type: string
                    port:
                      description: Target port
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    priority:
                      default: 100
                      description: Priority for path matching (higher = matched first)
                      format: int32
                      maximum: 1000
                      minimum: 1
                      type: integer
                  required:
                  - ip
                  - port
                  type: object
                type: array
              tunnelRef:
                description: Reference to the tunnel this resource belongs to
                properties:
                  name:
                    type: string
                  namespace:
                    description: Namespace of the referenced resource. If empty, defaults
                      to the same namespace as the referencing resource.
                    type: string
                required:
                - name
                type: object
            type: object
          status:
            description: |-
              PangolinResourceStatus defines the observed state of PangolinResource
              PangolinResourceStatus defines the observed state of PangolinResource
            properties:
              bindingMode:
                description: 'Binding mode: "Created" or "Bound"'
                type: string
              blockAccessEnabled:
                description: BlockAccessEnabled indicates if access is blocked until
                  authenticated
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              fullDomain:
                description: Full domain where resource is accessible
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation most recently
                  observed
                format: int64
                type: integer
              proxyEndpoint:
                description: Proxy endpoint for TCP/UDP resources
                type: string
              resolvedDomainId:
                description: Resolved domain ID from domain name
                type: string
              resourceId:
                description: Resource ID from Pangolin API
                type: string
              ssoEnabled:
                description: SSOEnabled indicates if SSO authentication is enabled
                  for this resource
                type: boolean
              status:
                description: 'Current status: Creating, Ready, Error, Deleting, Waiting'
                enum:
                - Creating
                - Ready
                - Error
                - Deleting
                - Waiting
                type: string
              targetCount:
                description: TargetCount is the number of targets configured for this
                  resource
                type: integer
              targetId:
                description: 'DEPRECATED: Keep for backward compatibility but don''t
                  use'
                type: string
              targetIds:
                description: All target IDs for this resource
                items:
                  type: string
                type: array
              url:
                description: Public URL for HTTP resources
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
